apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mypvc
spec:
  resources:
    requests:
      storage: 1.5Gi
  volumeMode: Filesystem
  volumeName: mypv
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
